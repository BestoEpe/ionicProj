import './Setup.css';

interface SetupProps {
  name: string;
}

const Setup: React.FC<SetupProps> = ({ name }) => {
  return (
    <div id="apuva">
      <p>testi</p>
    
    </div>

  );
};

export default Setup;
